// Eventos del formulario
// =================================

const formulary = document.querySelector('#formulario');

// formulario.addEventListener('submit', (e) => {
//     e.preventDefault();
//     console.log(e);
// });

formulary.addEventListener('submit', validarFormulary);

function validarFormulary(e) {
    e.preventDefault();
    console.log('jhsjkhfsjkfh')
}